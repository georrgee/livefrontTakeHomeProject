import { StyleSheet } from "react-native";
/** @description Styling for the MovieRating Atom Component */
export const styles = StyleSheet.create({
  starIconStyle: {
    position: "absolute",
  },

  ratingNumberTextStyle: {
    color: "#FFD700",
    fontSize: 16,
    fontWeight: "bold",
    fontFamily: 'Lexend',
    position: "absolute",
  }
})