/**
 * @param { number } rating The rating of the movie
 * @param { number } spacing The spacing between the rating and the star
 * @description Props for the MovieRating Atom Component
 */
export interface MovieRatingProps {
  rating:  number;
  spacing: number;
}