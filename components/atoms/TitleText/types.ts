import { StyleProp, TextStyle } from "react-native";
/** @description Props for the TitleText Atom component */

export interface TitleTextProps {
  children?: React.ReactNode
  style?: StyleProp<TextStyle>;
}
