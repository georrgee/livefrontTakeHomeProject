import { StyleSheet } from "react-native";
/** @description Styles for the TitleText Atom component */
export const styles = StyleSheet.create({

  title: {
    fontSize: 18,
    fontWeight: "700",
    color: "#fff",
  },
});