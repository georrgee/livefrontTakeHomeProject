import { StyleSheet } from "react-native";
/** @description Styles for the PosterImage Atom component */
export const styles = StyleSheet.create({
  placeHolder: {
    backgroundColor: '#222',
    justifyContent: 'center',
    alignItems: 'center',
  }
});